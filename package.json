{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "clear": "rimraf dist",
    "dev": "npm run clear && parcel src/*.html",
    "build": "npm run clear && parcel build src/index.html --public-url . --no-source-maps --no-minify"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "sass": "^1.29.0"
  },
  "dependencies": {
    "jquery": "^3.5.1",
    "moment": "^2.29.1"
  }
}
